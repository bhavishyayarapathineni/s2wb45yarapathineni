extends layout

block content
    h1= title       
        - var image_names= ["Abbott's babbler.jpg","Abert's Towhee.jpg","Pacific monarch.jpg"]         
            -for(let i=0;i<3;i++){
                - let randnum=Math.floor(Math.random()*5)
                 img(src= `/images/${image_names[randnum]}` alt="image", height="300", width="300")
            -}